# Contributing

Use focused changes, preserve component boundaries, run tests/lint/build before merging, and keep runtime/private data outside Git. Database changes must be represented through Prisma schema/migrations rather than uploading live database dumps.
