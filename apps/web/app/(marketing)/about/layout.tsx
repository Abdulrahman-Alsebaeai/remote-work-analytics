import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'About', description: 'Learn why Remote Work Intelligence exists and the principles behind its privacy-conscious product design.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
