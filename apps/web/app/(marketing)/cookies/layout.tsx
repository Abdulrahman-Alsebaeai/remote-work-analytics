import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Cookies Policy', description: 'How the website uses essential session cookies and local interface preferences.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
