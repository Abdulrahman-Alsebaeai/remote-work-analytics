import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Features', description: 'Explore productivity analytics, transparent activity signals, task context, screenshots, smart insights, and offline synchronization.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
