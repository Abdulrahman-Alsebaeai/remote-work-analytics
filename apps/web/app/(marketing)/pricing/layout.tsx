import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Pricing', description: 'Compare Free Trial, Professional, and Enterprise product tiers for Remote Work Intelligence.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
