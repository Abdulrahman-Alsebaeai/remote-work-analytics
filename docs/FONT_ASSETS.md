# Font assets

Font binaries are intentionally not bundled in this portable repository package. Install/source the font families referenced by the UI through their original distribution channels or replace them with your preferred licensed web/desktop fonts before packaging the desktop application.
